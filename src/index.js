import React from "react";
import ReactDOM from "react-dom/client";
import SafeCityDashboard from "./SafeCityDashboard";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <SafeCityDashboard />
  </React.StrictMode>
);
